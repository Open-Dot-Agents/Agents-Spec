version: 3

tasks:
  default:
    cmds:
      - task: verify

  verify:
    desc: Run specification release-readiness checks
    cmds:
      - python3 -c 'import jsonschema'
      - python3 conformance/run.py
